{
  "model_config": {
    "arch": "alexnet"
  },
  "optim_config": {
    "optimizer": "sgd",
    "base_lr": 0.1,
    "momentum": 0.9,
    "epochs": 100,
    "nesterov": true,
    "weight_decay": 1e-4,
    "scheduler": "multistep",
    "milestones": [
      80,
      90
    ],
    "lr_decay": 0.1,
    "batch_size": 256
  }
}

