{
  "model_config": {
    "arch": "rgbd_resnet"
  },
  "optim_config": {
    "optimizer": "adam",
    "base_lr": 1e-2,
    "betas": [0.9, 0.999],
    "epochs": 200,
    "eps": 1e-6,
    "weight_decay": 1e-5,
    "amsgrad": true,
    "scheduler": "multistep",
    "milestones": [
      80,
      150,
      190
    ],
    "lr_decay": 0.1,
    "batch_size": 128
  }
}